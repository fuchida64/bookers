<body>
	<p id="notice"></p>

	<p><strong>Title : </strong><%= @post.title %></p>
	<p><strong>Body : </strong><%= @post.body %></p>
	<div class="box"></div>

	<% id ='2' %>
	<%= link_to "Edit", edit_post_path(@post.id), class: "edit_#{id}" %>
	<p class="line">|</p>
	<%= link_to "Back", posts_path, class: "back"%>
</body>